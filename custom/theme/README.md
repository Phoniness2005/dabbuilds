# Custom theme / child theme

Place a WordPress **child theme** here when ready.

Typical layout:

```text
dabbuilds-child/
  style.css          # required theme header
  functions.php
  screenshot.png     # optional
```

Until Elementor Cloud theme files are exported or a child theme is uploaded, keep custom styles in `../css/` as the source of truth.
