# Elementor template exports

Export templates from WordPress (Elementor → Templates → Saved Templates → Export)
and commit the JSON files here so design structure is versioned.

Naming suggestion: `YYYY-MM-DD-homepage.json`
